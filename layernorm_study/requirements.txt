# EXACT pins, captured via `pip freeze` from the environment that
# actually produced every number in rounds 1, 1.5, and 2 (2026-08-28,
# mid-round-2, captured explicitly before the environment could be lost -
# see NOTES.md's "environment/provenance" note for why this mattered).
#
# Python 3.11.15. CPU only (no CUDA jaxlib installed - jax.default_backend()
# == "cpu", confirmed directly, not assumed - a GPU may be present on the
# host but nothing in this sub-project's environment can use it).
#
# To recreate: python3.11 -m venv layernorm_study/.venv && source
# layernorm_study/.venv/bin/activate && pip install -r
# layernorm_study/requirements.txt
#
# NOTE ON PLACEMENT: earlier in round 2 this environment lived in a
# session-scoped /tmp scratchpad directory, which is NOT persistent
# (confirmed directly - it was wiped once already mid-session) - moved
# here, inside the repo (gitignored, not committed itself - see
# layernorm_study/.gitignore's .venv/ entry), specifically so the
# environment itself survives even though it isn't tracked in git.

absl-py==2.5.0
aiofiles==25.1.0
contourpy==1.3.3
cycler==0.12.1
etils==1.14.0
flax==0.11.2
fonttools==4.63.0
fsspec==2026.7.0
humanize==4.16.0
iniconfig==2.3.0
jax==0.7.2
jaxlib==0.7.2
kiwisolver==1.5.1
markdown-it-py==4.2.0
matplotlib==3.11.1
mdurl==0.1.2
ml_dtypes==0.6.0
msgpack==1.2.2
numpy==2.4.6
opt_einsum==3.4.0
optax==0.2.8
orbax-checkpoint==0.12.4
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pluggy==1.6.0
prometheus_client==0.26.0
protobuf==7.36.0
psutil==7.2.2
Pygments==2.21.0
pyparsing==3.3.2
pytest==9.1.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
rich==15.0.0
s4-nnx @ git+https://github.com/binoygeorge97/s4-nnx.git@c706151e976c856b08fccfeaadc825e1e456e7c8
scipy==1.17.1
simplejson==4.1.2
six==1.17.0
tensorstore==0.1.85
treescope==0.1.10
typing_extensions==4.16.0
uvloop==0.22.1
zipp==4.1.0
