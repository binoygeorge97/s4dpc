# EXACT pins. SUPERSEDED 2026-09-01 - see the dated block below before
# trusting the Python version at the top of this comment for anything
# newer than round 3's original numbers.
#
# Original pins (rounds 1, 1.5, 2, and round 3's non-audit numbers):
# captured via `pip freeze` from the environment that actually produced
# those numbers (2026-08-28, mid-round-2, captured explicitly before the
# environment could be lost - see NOTES.md's "environment/provenance"
# note for why this mattered). Python 3.11.15, CPU only, host OS not
# recorded at the time (see NOTES.md's environment-provenance table -
# it was not Windows: `uvloop`, below, does not build there).
#
# ---------------------------------------------------------------------
# 2026-09-01 UPDATE - standardized on Python 3.12, not 3.11.
#
# Reason: round 3's audit session ran on a machine with no Python 3.11
# available (confirmed - not in `py -0p`'s list), so the audit used 3.12
# with these SAME `==` pins, which pip happily satisfied - jax==0.7.2 /
# flax==0.11.2 / optax==0.2.8 all ship cp312 wheels. That produced
# checkpoints that were NOT bit-identical to the original 3.11 runs
# despite identical seeds (train_mse differing 2-4x - see NOTES.md's
# "environment provenance" table for the determinism finding that
# isolated this to the environment, not to nondeterminism in the
# training path itself). Running two undocumented environments side by
# side is worse than picking one, so this project now standardizes on
# **3.12.1** going forward, deliberately, per this project's own
# precedent of raising a policy mismatch explicitly rather than quietly
# drifting (`layernorm_study/CLAUDE.md`'s TACC-vs-local decision, same
# pattern). All C1/C8-reconciliation numbers from 2026-09-01 onward were
# produced under this 3.12 pin set. Rounds 1-3's ORIGINAL numbers remain
# 3.11-sourced and are NOT bit-reproducible under this environment -
# treat any seed-indexed comparison that crosses the 2026-09-01 boundary
# as a different-checkpoint comparison, not a same-checkpoint one.
#
# `uvloop` is dropped: Windows-incompatible (`RuntimeError: uvloop does
# not support Windows`), and nothing in this project's own code imports
# it - it was `pip freeze` noise from whatever tooling produced the
# original list, not a real runtime dependency. `colorama`, `nest-asyncio`,
# `tzdata` are added: transitive deps pip pulled in on THIS platform that
# the original capture didn't need (Windows-vs-original-platform
# asymmetry, most likely - not investigated further, not load-bearing).
#
# To recreate: python3.12 -m venv layernorm_study/.venv && (on
# Windows) layernorm_study\.venv\Scripts\pip install -r
# layernorm_study/requirements.txt (on Unix) source
# layernorm_study/.venv/bin/activate && pip install -r
# layernorm_study/requirements.txt
#
# NOTE ON PLACEMENT: earlier in round 2 this environment lived in a
# session-scoped /tmp scratchpad directory, which is NOT persistent
# (confirmed directly - it was wiped once already mid-session) - moved
# here, inside the repo (gitignored, not committed itself - see
# layernorm_study/.gitignore's .venv/ entry), specifically so the
# environment itself survives even though it isn't tracked in git.

absl-py==2.5.0
aiofiles==25.1.0
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
etils==1.14.0
flax==0.11.2
fonttools==4.63.0
fsspec==2026.7.0
humanize==4.16.0
iniconfig==2.3.0
jax==0.7.2
jaxlib==0.7.2
kiwisolver==1.5.1
markdown-it-py==4.2.0
matplotlib==3.11.1
mdurl==0.1.2
ml_dtypes==0.6.0
msgpack==1.2.2
nest-asyncio==1.6.0
numpy==2.4.6
opt_einsum==3.4.0
optax==0.2.8
orbax-checkpoint==0.12.4
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pluggy==1.6.0
prometheus_client==0.26.0
protobuf==7.36.0
psutil==7.2.2
Pygments==2.21.0
pyparsing==3.3.2
pytest==9.1.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
rich==15.0.0
s4-nnx @ git+https://github.com/binoygeorge97/s4-nnx.git@c706151e976c856b08fccfeaadc825e1e456e7c8
scipy==1.17.1
simplejson==4.1.2
six==1.17.0
tensorstore==0.1.85
treescope==0.1.10
typing_extensions==4.16.0
tzdata==2026.3
zipp==4.1.0
